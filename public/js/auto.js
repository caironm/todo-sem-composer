var modal = UIkit.modal("#edit");

if ( modal.isActive() ) {
    modal.hide();
} else {
    modal.show();
}

var modal = UIkit.modal("#delet");

if ( modal.isActive() ) {
    modal.hide();
} else {
    modal.show();
}